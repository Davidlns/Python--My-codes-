# Desenvolva um programa que leia duas notas e calcule a media entre elas

nota1 = float(input('Nota 1: '))
nota2 = float(input('Nota 2: '))
media = (nota1 + nota2) / 2

print(f'A média entre {nota1} e {nota2} é {media}')